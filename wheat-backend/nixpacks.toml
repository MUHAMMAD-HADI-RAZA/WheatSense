[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "libGL", "glib"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m gunicorn flask_app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
